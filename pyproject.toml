[project]
name = "better-court-finder"
version = "0.1.0"
description = "A Better way to find courts"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.20.0.post0",
    "aiohttp>=3.11.18",
    "pydantic>=2.11.4",
    "python-dotenv>=1.2.2",
    "redis>=7.4.0",
    "toml>=0.10.2",
]
